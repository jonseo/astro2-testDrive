---
import { Markdown } from 'astro/components';
import Counter from './Counter.jsx';
---
<article>
<center><img src="https://americanshootingjournal.com/img/AMLogo.png" width="300" height="68" alt=""></center>
  <section>
    <Markdown>
      ## 🚀 
      Hell Yea! Get Sum!!
      
    </Markdown>
  </section>

  <!-- This Counter will only load the client side JS when it is visible on the page -->
  <Counter client:visible />

  <section>
    <p>
    
    </p>
  </section>
    
</article>

<style>
    article {
        padding-top: 2em;
        line-height: 1.5;
    }
    section {
        display: flex;
        flex-direction: column;
        gap: 1em;
        max-width: 70ch;
    }

    pre,
    code {
        font-family: var(--font-mono);
        background: var(--color-light);
        border-radius: 4px;
    }

    pre {
        padding: 1em 1.5em;
    }

    .tree {
        line-height: 1.2;
    }

    code:not(.tree) {
        padding: 0.125em;
        margin: 0 -0.125em;
    }
</style>
